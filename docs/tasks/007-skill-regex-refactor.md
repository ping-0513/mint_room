# 007: Skill Pack判定正規表現の構造化リファクタ

- Status: TODO
- 担当: Codex(思考最大)
- 目的: `server/skills.mjs` の1行1,000文字超の判定正規表現を読める部品から組み立てる構造へ整理し、パック追加・語彙修正時の退行リスクを下げる(PR #4 レビュー指摘M2)。

## スコープ

- 触ってよいファイル: `server/skills.mjs`, `server/skills.test.mjs`, `docs/tasks/007-skill-regex-refactor.md`, `docs/agent-handoff.md`(記載更新が必要な場合のみ)
- 触ってはいけないもの: 判定の入出力仕様(選択結果・APIレスポンス形状・UI)、`server/openai.mjs` の注入境界、他のSkill Pack仕様、既存テストの期待値

## 受け入れ条件

- 語彙のキーワード配列と小さな部品正規表現からプログラム的に判定パターンを組み立て、巨大な単一リテラルを廃止する
- 判定挙動は変えない(純リファクタ)。既存の判定テストが期待値の変更なしで全て通る
- 新しい語彙の追加が「配列に1項目足す」程度で済む構造にする
- 組み立て後のパターンでバックトラッキング爆発(ReDoS)を避ける考慮をコメントで明記する

## 制約

- 新規依存禁止。挙動変更禁止
- 新規・変更コードのコメントは日本語
- AGENTS.md 全ルール適用(特にスコープ外リファクタ禁止)

## 検証

- `npm test`(既存の判定テストが期待値変更なしで全て通ること)
- `npm run check` と `git diff --check`
- 変更前後で代表的な入力(料理依頼・メタ依頼・翻訳・無効化指示)の選択結果が一致することを確認し、確認方法を報告欄に記載

## 実装前メモ(担当が着手時に記入。小タスクは省略可)

## 質問(あれば。作業は可能な範囲で続ける)

## 報告(実装者が記入)

- 変更ファイル:
- 検証結果:
- 未確認・懸念:

## レビュー(相手モデルが記入)
