{
  "name": "mint-room",
  "version": "0.1.0",
  "private": true,
  "description": "Cute, practical, GPT-powered personal assistant web app (mint room). Zero-dependency Node server + static frontend.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check server/openai.mjs && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
