# Copy this file to .env.local and fill in your own values.
# Never commit your real .env.local file.

# OpenAI API key. Required for the chat API route to call OpenAI for real.
# Without it, the server route falls back to a mock response so the UI stays usable.
OPENAI_API_KEY=
